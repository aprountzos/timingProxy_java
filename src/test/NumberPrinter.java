package test;

/**
 * An interface that declares the printNumbers method.
 * Implementing classes will provide the logic to print numbers.
 *
 * @version 1.0
 * @since 2024-06-15
 */
public interface NumberPrinter {
    /**
     * Prints numbers from 1 to 10.
     */
    void printNumbers();
}
